// https://ecoo.mcpt.ca/problem/helloworld

#include <bits/stdc++.h>
#define watch(x) cerr << (#x) << " is " << (x) << endl;
using namespace std;
#define endl '\n'
signed main() {
    ios::sync_with_stdio(0);
    cin.sync_with_stdio(0);
    cin.tie(0);

    for(int i = 1; i <= 1e9; i++) {
        assert(true);
    }
    cout << "Hello, World!" << endl;
}