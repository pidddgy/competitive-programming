// https://codeforces.com/contest/1272/problem/D

#include <bits/stdc++.h>
using namespace std;

int main() {
    int N;
    cin >> N;
    int a[N+1];
    
    for(int i = 1; i <= N; i++) {
        cin >> a[i];
    }

    vector<vector<int>> b;

    vector<int> c;
    c.emplace_back(a[1]);
    for(int i = 2; i <= N; i++) {
        
    }
}