// Link

#include <bits/stdc++.h>
#define watch(x) cerr << (#x) << " is " << (x) << endl;
using namespace std;
#define endl '\n'
signed main() {
    ios::sync_with_stdio(0);
    cin.sync_with_stdio(0);
    cin.tie(0);
    srand(time(0));
    
    cout << 100 << endl;
    for(int i = 1; i <= 100; i++) {
        cout << 1 << " ";
    }
    cout << endl;
}