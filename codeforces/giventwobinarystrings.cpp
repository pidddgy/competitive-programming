// http://codeforces.com/contest/1202/problem/0

#include <bits/stdc++.h>
#define watch(x) cout << (#x) << " is " << (x) << endl;
using namespace std;
int main() {
    int T; 
    cin >> T;

    while(T--) {
        string x, y;
        cin >> x >> y;

        int a = stoi(x, nullptr, 2);
        int b = stoi(x, nullptr, 2);

        watch(a)
        watch(b)
    }
}