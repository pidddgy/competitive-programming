// http://codeforces.com/contest/1174/problem/B

#include <bits/stdc++.h>
#define pii pair<int,int>
#define fi first
#define se second
using namespace std;
int main() {
    cin.sync_with_stdio(0);
    cin.tie(0);

    int n;
    cin >> n;

    int a[n];
    for(auto &x: a) cin >> x;

    // .fi = indice, .se = value
    vector<pii> even;
    vector<pii> odd;
    for(auto x: a) {

    }
}