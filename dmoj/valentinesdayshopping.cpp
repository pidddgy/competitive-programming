// https://dmoj.ca/problem/mccc1s4

#include <bits/stdc++.h>
using namespace std;
int main() {
    int N, Q, B;
    cin >> N >> Q >> B;

    int a[N+1];
    for(int i = 1; i <= N; i++) {
        cin >> a[i];
    }

    for(int i = 1; i <= Q; i++) {
        char query;
        cin >> query;
    }
}